/**
 * One place for the things both auth screens show, so they cannot drift.
 *
 * The photo is an Unsplash URL rather than a checked-in asset because nothing in
 * this app needs a build step for it, and it was verified by fetching rather than
 * trusted from memory.
 */
export const BRAND_IMAGE =
  'https://images.unsplash.com/photo-1585110396000-c9ffd4e4b308?auto=format&fit=crop&w=1400&q=70';

export const BRAND_TAGLINE = 'Healthy rabbits · Better farms · Brighter future';
